
<% @posts.each do |post| %>
    <div class="post_wrapper">
      <h2 class="title"><%= link_to post.title, post %></h2>
      <p class="date">Created: <%= post.created_at.strftime "%B, %d, %Y" %> - <%= pluralize(post.comments.count, 'comment') %></p>
    </div>
<% end %>